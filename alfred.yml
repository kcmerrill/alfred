example:
    summary: This is the summary
    command: |
        true

entrypoint:
    multitask: sleep1 sleep3
    tasks: |
        example
    command: ls -alh /doesnotexist 

server:
    task: serve
    wait: 1h

serve:
    serve: 8089

sleep: 
    command: sleep {{ index .Args 0 }} && echo slept {{ index .Args 0 }}

test.args:
    tasks: |
        sleep(1)
    command: echo {{ index .Args 0 }}
